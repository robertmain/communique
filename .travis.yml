language: php

sudo: false

branches:
    only: 
        - master

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - 7

before_install:
  - composer self-update
  - composer update

install:
  - composer install